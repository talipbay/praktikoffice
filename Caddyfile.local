# Caddyfile for local development
# Use this for testing locally without SSL

localhost:80 {
    encode gzip

    # Reverse proxy to Next.js app running on port 3000
    reverse_proxy localhost:3000

    # Logging
    log {
        output stdout
        format console
    }
}
